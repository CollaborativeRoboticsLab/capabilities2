# the empty provider
%YAML 1.1
---
name: PromptPlannerRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the prompting for an execution plan. This can be used requesting a new plan at the beginning or when 
              new data is available"
implements: capabilities2_runner_prompt/PromptPlannerRunner
runner: capabilities2_runner::PromptPlannerRunner
depends_on:
  "capabilities2_runner_prompt/PromptLaunchRunner":
    provider: "capabilities2_runner_prompt/PromptLaunchRunner"