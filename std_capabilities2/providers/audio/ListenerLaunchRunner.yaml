# the empty provider
%YAML 1.1
---
name: ListenerLaunchRunner
spec_type: provider
spec_version: 1.1
description: "The capability provider for the audio stack"
implements: std_capabilities2/ListenerLaunchRunner
runner: launch/audio/listen.launch.py
